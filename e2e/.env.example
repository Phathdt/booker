# Booker E2E Testing Environment Variables

# Test environment (local, ci, debug)
TEST_ENVIRONMENT=local

# Application URL (Booker web frontend via Traefik)
APP_URL=http://booker.localhost

# Test user credentials (seeded via `make seed`)
TEST_USER_EMAIL=trader1@booker.dev
TEST_USER_PASSWORD=password123
